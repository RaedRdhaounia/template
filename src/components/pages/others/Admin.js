import { Typography } from "@mui/material";
import React from "react";

const Admin = () =>{
return(
    <Typography>

    this is some instranction for the admin user in general 
    </Typography>
)
}
export default Admin